# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /v1/posts/:id      controllers.PostController.show(id:String)
POST    /v1/posts          controllers.PostController.create()
